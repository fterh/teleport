{
  "name": "teleport",
  "version": "1.0.0",
  "description": "Singapore-based Telegram bot serving consolidated traffic and transport data.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "export NODE_ENV=test || SET NODE_ENV=test && jest tests/*.test.js",
    "test-watch": "nodemon --exec 'npm test'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^5.0.1",
    "telegraf": "^3.19.0"
  }
}
